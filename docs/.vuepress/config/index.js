const themeCof = require('./theme');
const markdownCof = require('./markdown');
const baseCof = require('./base');
const webpackCof = require('./webpack');
module.exports = {
    baseCof,
    themeCof,
    markdownCof,
    webpackCof,
}